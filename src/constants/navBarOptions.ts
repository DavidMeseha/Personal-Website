export type NavOptions =
  | "intro"
  | "skills"
  | "portfolio"
  | "interested"
  | string;

const navBarOptions: NavOptions[] = [
  "intro",
  "skills",
  "portfolio",
  "interested",
];

export default navBarOptions;
